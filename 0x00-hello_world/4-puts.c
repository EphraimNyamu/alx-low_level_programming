#include <stdio.h>
/**
 * main- a program to print a message with puts function
 * Return: value should be 0 for successful compilation
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
