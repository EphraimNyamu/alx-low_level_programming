#include <stdio.h>
/**
 * main - c program to print proper grammar
 * followed by a new line using printf function
 * Return: successful compilation return value is 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
